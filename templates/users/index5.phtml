<form action="/users5" method="get">
    <input type="search" required name="term" placeholder="Enter first part of Name"  value="<?= htmlspecialchars($term) ?>">
</form>
<table>
    <?php foreach ($searched as $user): ?>
        <div>
            <?= htmlspecialchars($user['firstName']) ?>
        </div>
    <?php endforeach ?>
</table>